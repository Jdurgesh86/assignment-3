#include<stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    if(num%2==0 && num%2==0)
       printf("the number is divisible by 3 and 2");
    else
       printf("the number is not divisible by 3 and 2");
}